package ht.util.testframework;

/**
 * Tag a class with this so that it can be found.  EnhancedTestCase already is tagged so if you subclass your fine. This
 * is good if your using JUnit4+ calling convention
 */
public interface HTTestRunnable {
}
