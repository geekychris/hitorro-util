package ht.util.core.params.propreaders;

/**
 * Type of directory used in the directory props reader (shortcut to select which environmentally controlled dir to use
 * such as bin or home.
 */
public enum DirectoryType {
    Home, Bin, Other
}