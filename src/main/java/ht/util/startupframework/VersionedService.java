package ht.util.startupframework;

/**
 * manage upgrading any meta information used by a service through an upgrade framework during startup. All init
 * messages will be sent, followed by upgrade
 */
public interface VersionedService {
}
